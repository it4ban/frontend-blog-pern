import React from 'react';

const Post = ({ id, title, user, createdAt, viewCount, commentsCount, tags, isEditable }) => {
	const onClickRemove = () => {};

	return (
		<div className="bg-white border border-solid border-gray-200 rounded-md overflow-hidden mb-4 relative">Post</div>
	);
};

export default Post;
